# Configuração do PostgreSQL - COPIE PARA .env.local
DATABASE_URL=postgresql://usuario:senha@host:5432/postgres

# Configurações de JWT para autenticação
JWT_SECRET=sua-chave-secreta-jwt-super-segura
JWT_EXPIRY=7d

# Configurações da aplicação
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=sua-chave-secreta-nextauth

# Configurações de segurança (produção)
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Configurações de Performance
# Limite de tamanho de payload (em MB) - padrão é 1MB
# BODY_SIZE_LIMIT=4mb 